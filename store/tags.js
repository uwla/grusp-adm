import { createActions, createMutations, createState } from '../utils'

export const state = createState('tags')
export const mutations = createMutations('tags')
export const actions = createActions('tag')