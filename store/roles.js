import { createMutations, createActions, createState } from '../utils'

export const state = createState('roles')
export const mutations = createMutations('roles')
export const actions = createActions('/role')
