import { createMutations, createActions, createState } from '../utils'

export const state = createState('categories')
export const mutations = createMutations('categories')
export const actions = createActions('/category')
